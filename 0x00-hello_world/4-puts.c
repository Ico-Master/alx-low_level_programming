#include <stdio.h>

/**
 * main - prints the phrase "Programming is like building a multiple puzzle
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a puzzle");
        return (0);
}
